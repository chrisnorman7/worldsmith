import '../../../world_context.dart';
import '../../level/credits_menu.dart';

/// Returns a menu that will show credits.
CreditsMenu getCreditsMenu(WorldContext worldContext) =>
    CreditsMenu(worldContext);
